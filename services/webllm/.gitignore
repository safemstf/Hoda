node_modules/
.claude/
*.log
.DS_Store
dist/

# Bundled files (generated by build script)
# Previously ignored to reduce repo size — temporarily unignored so the built
# WebLLM bundle can be shared and used by teammates without running the build script.
# src/bundled-webllm.js
# src/bundled-webllm.js.map